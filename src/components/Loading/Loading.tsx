import React, { FC } from "react";
import { LoadingContainer, LoadingIcon } from "./Loading.style";

const Loading: FC = () => {
    return(
        <LoadingContainer>
            <LoadingIcon/>
        </LoadingContainer>
    )
}

export default Loading;